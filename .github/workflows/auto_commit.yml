name: Hourly Commit

on:
  schedule:
    - cron: "0 * * * *"  # Запускается каждый час в начале часа
  workflow_dispatch:  # Позволяет запускать вручную

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  # Клонирует репозиторий

      - name: Configure Git
        run: |
          git config --global user.name "Gnarkilll"
          git config --global user.email "vpcorwin@gmail.com"

      - name: Debug GH_PAT
        run: |
          if [ -z "${{ secrets.GH_PAT }}" ]; then
            echo "❌ GH_PAT is EMPTY! Проверь, что он передаётся в Secrets."
            exit 1
          else
            echo "✅ GH_PAT is set."
          fi

      - name: Debug Git User
        run: |
          git config --list  # Выведет текущего пользователя Git

      - name: Make changes
        run: |
          echo "Auto-commit: $(date)" >> activity_log.txt
          git add activity_log.txt
          git commit -m "Auto-commit $(date)" || exit 0  # Не ломает workflow, если нечего коммитить

      - name: Push changes
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git remote set-url origin https://${GH_PAT}@github.com/${{ github.repository }}.git
          git push origin main  # Проверь, что у тебя правильная ветка!
